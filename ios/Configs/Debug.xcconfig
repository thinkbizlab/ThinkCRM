// Debug — points at a locally running Fastify server. Override per-machine by
// dropping a Debug.local.xcconfig next to this file (gitignored).
#include? "Debug.local.xcconfig"
WORK_CRM_API_BASE_URL = http:/$()/localhost:3000/api/v1
